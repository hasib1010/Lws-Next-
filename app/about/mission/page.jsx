import React from 'react';

const mission = () => {
    throw new Error('The is an error')
    return (
        <div>
            
            this is mission
        </div>
    );
};

export default mission;